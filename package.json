{
  "name": "sinkroo",
  "version": "0.1.0",
  "private": true,
  "description": "Sinkroo — Autonomous AI Marketing Infrastructure (monorepo)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.8.0"
}
